/*
const mockRequest = ()=>{
    return{
        session:{
            user:undefined
        }
    }
}

const mockResponse = ()=>{
    let res:any = {}
    res.status =jest.fn().mockReturnValue(res)
    res.send = jest.fn().mockReturnValue(req)

}

describe ('authenticationMiddleware', ()=>{
    let req;
    let res;
    let next;
    it('Should not allow someone who was not logged in thruoguh', ()=>{

    })
})
*/